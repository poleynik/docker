input {
  beats {
    port => 5048
  }
}

output {
  elasticsearch {
    index => "%{[@metadata][beat]}-%{[@metadata][version]}-netflow-%{+YYYY.MM.dd}"
    hosts => ["http://10.101.49.4:9200", "http://10.101.49.3:9200"]
    user => 'logstash_writer'
    password => 'Streak7_Monotype_Spirit'
  }
    #stdout { codec => rubydebug }
}
